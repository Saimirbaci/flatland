# Clean baseline: no FaultInjector. The trajectory here is the reference the
# fault worlds are compared against (clean-run invariant).
properties: {}
layers:
  - name: "2d"
    map: "../diff_drive_tests/map.yaml"
    color: [0, 1, 0, 1]
models:
  - name: robot
    pose: [0, 0, 0]
    model: "robot.model.yaml"
